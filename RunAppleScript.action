if input volume of (get volume settings) = 0 then
set level to 100
display notification "Unmuted" with title "Microphone" sound name "Tink"
else
set level to 0
display notification "Muted" with title "Microphone" sound name "Tink"
end if

set volume input volume level
